{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile --ignore-scripts && pnpm --filter @workspace/api-server build",
  "buildCommand": "echo API build already completed",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
