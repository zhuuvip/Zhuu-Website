{
  "version": 2,
  "buildCommand": "pnpm tsc --build && pnpm --filter @workspace/zhuu-vip run build",
  "outputDirectory": "artifacts/zhuu-vip/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
